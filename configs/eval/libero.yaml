evaluation:
  horizon: 14
  max_steps: [25, 25, 25, 95]
  task_suites:
    - libero_10
    - libero_object
    - libero_goal
    - libero_spatial
  num_episodes: 3
  seed: 42

server:
  url: "ws://0.0.0.0:9003"

checkpoint:
  name: "Evo1_libero_all"

logging:
  log_dir: "./log_file"
  video_dir: "./video_checkpoint_distill_stage1_fullfinetuning_v02-2"
  video_fps: 30
debug: False